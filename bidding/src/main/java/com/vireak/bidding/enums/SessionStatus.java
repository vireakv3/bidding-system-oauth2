package com.vireak.bidding.enums;

public enum SessionStatus {
    ACTIVE,     // The bid session is currently open and accepting bids.
    CLOSED      // The bid session has ended and is no longer accepting bids.
}